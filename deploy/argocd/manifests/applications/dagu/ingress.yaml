apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  labels:
    app.kubernetes.io/name: dagu
  name: dagu
  namespace: todo
spec:
  ingressClassName: traefik
  rules:
  - host: dagu.scottseo.tech
    http:
      paths:
      - backend:
          service:
            name: dagu
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - dagu.scottseo.tech
    secretName: scottseo-tech-tls-dagu