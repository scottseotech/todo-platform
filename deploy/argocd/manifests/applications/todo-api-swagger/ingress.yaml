apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: traefik
  labels:
    app.kubernetes.io/name: todo-api-swagger
  name: todo-api-swagger
  namespace: todo
spec:
  ingressClassName: traefik
  rules:
  - host: todo-api-swagger.scottseo.tech
    http:
      paths:
      - backend:
          service:
            name: todo-api-swagger 
            port:
              number: 8080 
        path: /
        pathType: Prefix